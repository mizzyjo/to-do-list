import React from 'react';

export default function Navbar() {
  return (
    <div>
      <button>Mode</button>
      <button>All</button>
      <button>Active</button>
      <button>Completed</button>
    </div>
  );
}

